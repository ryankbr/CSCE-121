#include <iostream>
using std::cout; using std::endl; using std::cin;

int main () {
    /*
    int numbers[] = {1, 3, 5};
    int* ptr = numbers;
    cout << ptr[0] << endl << ptr[1] << endl << ptr[2] << endl;

    // a pointer points to the first element of an array
    // but when using indices to output, they are the exact same

    cout << "is the same as" << endl;
    cout << numbers[0] << endl << numbers[1] << endl << numbers[2] << endl;
    */

   

return 0;
}