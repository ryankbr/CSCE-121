#include <iostream>
using std::cout;
using std::cin;
using std::endl;

int sumDigits {

}

int main() {

    int number = 0;
    cout << "Enter a non-negative integer: ";
    cin >> number;
    cout << "The sum of the digits is: " << sumDigits(number);
}